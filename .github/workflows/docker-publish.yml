name: Docker

on:
  release:
    types: [published]
    branches:
      - master

    # Publish `v1.2.3` tags as releases.
   # tags:
   #   - ${GITHUB_REF}
    #  - latest

env:
  IMAGE_NAME: hhaluk/crypto-watchdog

jobs:
  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v2

      - name: Prepare Release Version Tag
        run: |
          IMAGE_ID=docker.pkg.github.com/${{ github.repository }}/$IMAGE_NAME
          # Change all uppercase to lowercase
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')

          # Strip git ref prefix from version
          VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')

          # Strip "v" prefix from tag name
          [[ "${{ github.ref }}" == "refs/tags/"* ]] && VERSION=$(echo $VERSION | sed -e 's/^v//')

          echo IMAGE_ID=$IMAGE_ID
          echo VERSION=$VERSION
          echo $IMAGE_NAME $IMAGE_ID:$VERSION
          docker image ls

      - name: Build image
        run: docker build . --file Dockerfile --tag $IMAGE_NAME:latest --tag $IMAGE_NAME:VERSION 

      - name: Log into registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login docker.pkg.github.com -u ${{ github.actor }} --password-stdin

      - name: Push DockerHub
        run: |
          docker image ls
          # docker image push --all-tags $IMAGE_NAME
          # docker push $IMAGE_ID:$VERSION
        
